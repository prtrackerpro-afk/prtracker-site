# Handoff — Admin Dashboard pronto pra ativar (29/04/2026 madrugada)

## TL;DR

Construí o admin dashboard end-to-end durante a madrugada. Branch pronta + push feito. **Falta abrir o PR (1 clique) e fazer o setup de ~5min.**

## Ações imediatas ao acordar (ordem):

### 1. Abrir o PR no GitHub (30 segundos)

Click neste link — vai abrir a página de criar PR já pré-preenchida:

👉 **https://github.com/prtrackerpro-afk/prtracker-site/pull/new/feat/admin-dashboard**

O título e body sugeridos estão em `docs/PR-BODY.md` se quiser revisar antes de criar. Senão é só clicar **"Create pull request"**.

### 2. Validar o preview do Vercel

Assim que o PR for criado, Vercel vai fazer build automático de preview. **Vai falhar** com erro tipo `SUPABASE_URL missing` — esperado, falta setar env vars (próximo passo).

Pode ignorar a falha do preview por enquanto e seguir pro setup.

### 3. Setup completo (5 min) — segue [docs/ADMIN.md](./ADMIN.md)

Resumo dos passos:

1. **Criar projeto Supabase** (free tier) → rodar `supabase/migrations/0001_init.sql` → criar user `contato@prtracker.com.br` com senha `MYpr171##`
2. **(Opcional) Criar conta Resend** se quiser email de alertas críticos
3. **(Opcional) Gerar API key Windsor.ai** se quiser GA4 ingerido
4. **Setar env vars no Vercel** (lista completa em `.env.example`)
5. **Mergear o PR** → Vercel reconstrói prod
6. **Disparar primeira ingestão** via curl com `CRON_SECRET`

### 4. Acessar pela primeira vez

`https://prtracker.com.br/admin` → tela de login → email + senha → dashboard.

Após primeira ingestão, vai ter:
- ~30 dias de Meta Ads insights
- GA4 daily metrics se Windsor configurado
- Alertas avaliados nas últimas 24h

### 5. (Opcional) Apontar admin.prtracker.com.br

Adicionar CNAME no DNS provider:
```
admin → cname.vercel-dns.com
```
Depois Vercel **Settings → Domains → Add `admin.prtracker.com.br`**.

## O que está incluso

✅ Login email/senha · whitelist por email · cookies HTTP-only
✅ 5 dashboards: Overview / Meta / GA4 / Vendas / Alertas
✅ Schema Supabase completo + RLS + audit log
✅ Cron diário 03:23 BRT pulla Meta + Windsor + avalia alertas
✅ Email Resend opcional pra alertas críticos
✅ UI brand-themed (navy + lime, Tailwind 4)
✅ Charts interativos (Recharts) com hover/tooltip
✅ Filtros de data 1d/7d/14d/30d/90d
✅ Drill Meta: campaign → adset → ad
✅ Mobile responsive (sidebar collapsa)
✅ Headers de segurança (X-Frame-Options, etc)
✅ Cron protegido por Bearer secret
✅ Site existente NÃO mexido — homepage/produtos/checkout 100% iguais

## O que ficou de fora (próxima iteração)

⏳ Custom domain admin.prtracker.com.br (precisa CNAME no DNS — instruções no doc)
⏳ Resend custom domain `noreply@prtracker.com.br` (precisa DNS — usa `onboarding@resend.dev` por padrão)
⏳ Webhook MP → tabela `sales` automaticamente
⏳ Conexão real TikTok / ML / Amazon / Shopee no Windsor (placeholders agora)
⏳ Breakdowns Meta (placement, age, gender)
⏳ Funil de conversão visual

## Rollback se algo der errado

Site existente não foi modificado. Se precisar dar rollback:

```bash
git checkout main  # volta pra main intacta
# Vercel volta pro último deploy de main
```

O branch `feat/admin-dashboard` fica preservado pra retomar depois.

## Estado do C1 (campanha Meta lançada ontem 20:30)

🚀 **C1 ativa rodando** — `CONV_GERAL_MAI-2026_LANC` PAUSED→ACTIVE em 28/04 ~22h
- A1 + A2 ACTIVE no adset BROAD-AdvantagePlus
- R$50/dia CBO
- Tráfego ABO PAUSED (liberado o budget)
- Andromeda em Learning Phase (esperado)

Quando o admin estiver online + ingerido, vai ver os primeiros dados da C1 nos dashboards.

## Memória atualizada

Entradas novas em `~/.claude/projects/.../memory/`:
- `project_admin_dashboard_apr2026.md` (em breve — vou criar)
- `project_budget_cap_apr2026.md` (já existe — R$50/dia limit)
- `reference_meta_api_block_transient.md` (já existe — atualizado)
